apiVersion: v1
kind: ResourceQuota
metadata:
  name: pod-demo
spec:
  hard:
    pods: "2"



# kubectl run nginx --image=nginx --restart=Never --namespace=develoment
# kubectl run tomcat --image=tomcat:8.0 --namespace=development
# kubectl run -i --tty busybox --image=busybox --restart=Never --namespace=development
